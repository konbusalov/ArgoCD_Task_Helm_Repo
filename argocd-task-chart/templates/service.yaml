apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-task-chart.fullname" . }}
  labels:
    {{- include "argocd-task-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-task-chart.selectorLabels" . | nindent 4 }}
